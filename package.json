{
  "name": "flower-shop",
  "version": "1.0.0",
  "description": "A simple react flower shop app",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ValeriaRudenko/Flower-Shop.git"
  },
  "author": "Valeriia Rudenko",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "faker": "^6.6.6"
  }
}
